[project]
name = "deepcontext-site"
version = "0.1.0"
description = "Static site generator for the DeepContext.com node graph"
license = "BSD-2-Clause-Patent"
license-files = ["LICENSE"]
requires-python = ">=3.11"
dependencies = [
    "markdown>=3.6",
]

[tool.uv]
managed = true
